/* Exercise 1 - 1. 
 * Run the ``hello, world`` program on your system. 
 * Experiment with leaving
 * out parts of the program, to see what error messages you get.
 */

// If any character from the printf statement is missing, the program won"t compile and we
// get a compiler error. That applies for any missing character except characters within the double quotes. 

#include <stdio.h>

int main()
{
    printf("hello, world\n");

}